import React from 'react'

type VolumeControlProps = {}

export const VolumeControl = (props: VolumeControlProps) => {
  return (
    <div>The volume control feature is not yet implemented.</div>
  )
}