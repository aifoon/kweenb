{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "baseUrl": "../../src",
    "paths": {
      "@shared/*": ["shared/*"]
    },
  },
  "include": [
    "../shared/**/*.ts",
    "src/**/*.ts",
    "exposedMainWorld.d.ts"
  ],
  "exclude": ["dist/**/*.*"]
}