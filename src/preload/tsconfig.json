{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "baseUrl": "../../src",
  },
  "include": [
    "src/**/*.ts",
    "**/*.d.ts",
    "exposedMainWorld.d.ts",
  ],
  "exclude": ["dist/**/*.*"]
}