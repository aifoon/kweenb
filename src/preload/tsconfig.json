{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "baseUrl": "../../src",
    "outDir": "./dist",
  },
  "include": [
    "src/**/*.ts",
    "**/*.d.ts",
    "exposedMainWorld.d.ts",
  ],
  "exclude": ["dist/**/*.*"]
}